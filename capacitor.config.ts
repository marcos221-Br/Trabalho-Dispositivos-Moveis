import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'trabalho.desenvolvimento.dipositivos.moveis',
  appName: 'trabalho-desenvolvimento-dipositivos-moveis',
  webDir: 'www'
};

export default config;
